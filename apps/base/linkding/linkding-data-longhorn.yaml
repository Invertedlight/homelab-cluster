apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-data-longhorn
  namespace: linkding
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
